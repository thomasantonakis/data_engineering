# Load required libraries

# load library for google Authentiction
library(googleAuthR)

# Load library for manipulating data from Google Analytics
library(googleAnalyticsR)

# Load library for retrieving data from the Webmasters console
library(searchConsoleR)

# load library for communication with BigQuery
library(bigrquery)

# load library for Google Drive authentication
library(googledrive)

# load library that will help us with manipulation
library(sqldf)

# Load read R because I dont know
library(readr)

#Load library for string manipulation
library(stringr)

# Load tibble for dataframe manipulation
library(tibble)

# Load Library for Adwords
library(RAdwords)
